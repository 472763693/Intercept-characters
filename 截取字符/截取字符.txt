//根据索引返回字符
charAt();

//根据字符返回索引
indexOf(); //从前往后
lastIndexOf(); //从后往前


//截取字符串
//1个参数 从索引为3的位置开始 截取后面的所有字符
slice(3); 

//2个参数 从索引为2的位置开始 截取到索引为5的字符 不含5
slice(起点， [终点]);
slice(2, 5);

//1个参数 从索引为3的位置开始 截取后面的所有字符
substr(3);

//2个参数 从索引为2的位置开始[以索引2为第1个] 向后取5个
substr(起点， [终点]);
substr(2, 5);



